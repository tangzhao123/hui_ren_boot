<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.gson.hui_ren_boot.model.mapper.drug.ThroughMapper">

    <!-- 新增仓库明细 -->
    <insert id="addThrough">
        insert into through values (seq_huiren.nextval,#{throughDeport},#{throughEntry})
    </insert>
</mapper>